import 'package:flutter/material.dart';

class VanEntryScreen extends StatelessWidget {
  const VanEntryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Manual Entry'),
      ),
      body: const Center(
        child: Text('Manual Entry Screen - Coming Soon'),
      ),
    );
  }
}
